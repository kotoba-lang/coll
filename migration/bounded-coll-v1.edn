{:schema :kotoba.coll-migration/v1
 :profile :kotoba.coll/bounded-v1
 :legacy
 {:source "src/kotoba/lang/coll.cljc"
  :status :retained-general-oracle}
 :canonical
 {:source "src/kotoba/lang/bounded_coll.kotoba"
  :authority :kotoba
  :required-capabilities []}
 :ported
 {:string-set [:empty :count :contains :add :remove :equal]
  :keyword-set [:count :contains :add :remove :equal]}
 :retained
 {:higher-order-map "Stored closures and generic map ABI values are not sealed."
  :deep-merge "Requires bounded recursive document/map semantics."
  :index-by "Requires a generic item/key function contract."
  :walk "Requires bounded document traversal and visitor dispatch."
  :set-algebra "Bounded recursion plus typed-set-nth passes admission and Wasm,
                but restricted JS rejects an unsupported KIR node (measured
                2026-08-02); union/intersection/difference remain deferred."}
 :bounds
 {:typed-set-items 32
  :effects []}
 :verification
 {:self-check-main 42
  :required-targets [:js-browser-kotoba-v1 :wasm32-browser-kotoba-v1]
  :local-result
  {:date "2026-08-02"
   :pure-product-check :pass
   :js-browser {:compile :pass :main 42}
   :wasm32-browser {:compile :pass :instantiate :pass :main 42}
   :required-capabilities []}}}
